import React from "react";
import styles from "./Footer.module.scss";

const Footer = () => (
  <div className={styles.wrapper}>
    <h4 className={styles.text}>Running Water's Portfolio</h4>
  </div>
);

export default Footer;
